conf folder includes the configurations of the experiment
models folder includes the models used in the project
train.py is used for training and evaluating the models
utils.py contains utility functions (e.g., loading data and models)